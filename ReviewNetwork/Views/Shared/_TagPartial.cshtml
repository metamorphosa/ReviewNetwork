@model LikeViewModel

<div class="row">
    @foreach(var tag in Model.Tags)
    {
        <div class="col-sm-6">
          <div class="card">
            <div class="card-body">
                <h5 class="card-title">@tag.Name</h5>
            </div>
          </div>
        </div>
    }
</div>